# lambda function is a short syntax for writing functions just like arrow functions
# we can give it multiple parameters and pass another function as an argument as well
# function which take other function as an argument is called an highr order function
add = lambda x: x + 2

print(add(3))
